root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  exclude_dir = ["tmp", "vendor"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  delay = 1000
  stop_on_error = true

[log]
  main_only = false
  time = true

[misc]
  clean_on_exit = true
